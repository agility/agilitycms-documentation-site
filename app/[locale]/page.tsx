/**
 * The home page is just the catch-all with no slug (demosite2025 pattern —
 * even the root page is CMS-driven).
 */
export { generateMetadata } from "./[...slug]/page";
export { default } from "./[...slug]/page";
